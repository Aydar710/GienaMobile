<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gina.gienamobile">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".presentation.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GienaMobile"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".presentation.main.MainActivity"
            android:exported="true">


        </activity>

        <activity
            android:name=".presentation.entername.EnterNameActivity"
            android:exported="true">

        </activity>

        <activity
            android:name=".presentation.segmentation.SegmentationActivity"
            android:exported="true" />

        <activity android:name=".presentation.endgame.EndGameActivity" />

        <activity
            android:name=".presentation.greeting.GreetingActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>